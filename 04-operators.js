//
// post increment and pre-increment 
let a = a + 1;

let b = 3;
b++;     // post-increment.  -- after the line + 1
console.log(b)

let c = 5;
++c;     // pre-increment   -- before the line  + 1
console.log(++C, )

// shorthand for increment and decrement 
let z = 10;
z += 1;   // same as z = z + 1
z -= 10;  // smae as z = z -10