// Unary (only one data, or one operand)
console.log(! true);
// binary (only two data)
console.log(3+3);
// Ternary  -> three data

let a = 10;
if (a>0) {
    console.log("a is postive")
} else {
     console.log("a is negative")
}

// Ternary operator consits of two symbols  ? and :
// <condi> ?(value_if_true) :(value_if_cond_false)
console.log( a > 0 ?"a is positive" :"a is negative");

